#!/bin/bash

cd $HOME
#nohup git daemon --base-path=/u01/dev/android/git --base-path-relaxed  --export-all &
nohup git daemon --base-path=/u01/dev/android/git --base-path-relaxed  --export-all --enable=receive-pack &

exit 0

